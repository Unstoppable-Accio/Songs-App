import songsData from "../../data/songData";
// import { SONGS_LIST } from "../actions/actionTypes";


const initialState = songsData 

const songListReducer = (state = initialState, action) => {
        // switch (action.type) {
        //     // case SONGS_LIST: return  action.payload
        //     default: return state
        // }
        return state
}

export default songListReducer;




